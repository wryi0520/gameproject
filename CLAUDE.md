# CLAUDE.md

## 이슈 → 워크트리 → PR → 리뷰 워크플로우

이 저장소에서 기능/버그 작업을 진행할 때는 다음 순서를 따른다.

1. **이슈 생성**: 작업할 내용을 GitHub 이슈로 먼저 등록한다 (`gh issue create`). 이슈에는 배경과 체크리스트를 포함한다.
2. **워크트리 생성**: 이슈별로 별도의 git worktree를 만들어 작업한다. 메인 체크아웃이나 다른 작업과 섞이지 않도록 격리한다.
3. **작업 및 커밋**: 워크트리 안에서 변경 사항을 작업하고, 커밋 메시지 본문에 `Closes #<이슈번호>`를 포함해 이슈와 연결한다.
4. **PR 생성**: 브랜치를 origin에 push하고 `gh pr create`로 PR을 연다. PR 본문에도 `Closes #<이슈번호>`를 포함해 머지 시 이슈가 자동으로 닫히도록 한다.
5. **리뷰 및 머지**: PR 상태(`mergeable`, `mergeStateStatus`)를 확인한 뒤 머지한다 (기본: squash + 브랜치 삭제). 머지 전 사용자 확인을 받는다.
6. **워크트리 정리**: 머지가 끝나면 해당 워크트리는 제거한다. 단, 워크트리에 병합되지 않은 커밋이 남아 있으면 삭제 전 사용자에게 확인한다.
